# runs

Immutable per-run snapshots: resolved config, source YAMLs, metadata.json
(git commit, seed, device), metrics.json, and copied checkpoints. Never modify
past runs when the active config changes.
