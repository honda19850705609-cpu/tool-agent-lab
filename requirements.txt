# core
torch
transformers>=4.45
accelerate
# config-first infrastructure
PyYAML>=6.0
# fine-tuning
peft
trl>=0.11
datasets
# quantized loading (optional, for low-VRAM)
bitsandbytes
# domestic (China-direct) dataset/model hub + its deps
modelscope
addict
simplejson
sortedcontainers
